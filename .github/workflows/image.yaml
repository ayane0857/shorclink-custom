name: Docker Build and Push
on:
  push:
    tags:
      - "v*"

jobs:
  BuildAndPush:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Go modules cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Extract tag name
        run: |
          tagname=${GITHUB_REF#refs/*/} && echo "TAG_NAME=${tagname#v}" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1
        with:
          registry: registry.uniproject.jp
          username: ayane
          password: ${{ secrets.HARBOR_TOKEN }}

      - name: Build Docker image
        run: docker build --progress=plain -t registry.uniproject.jp/ayane/shortlink:${{ env.TAG_NAME }} .

      - name: Push Docker image
        run: docker push registry.uniproject.jp/ayane/shortlink:${{ env.TAG_NAME }}
